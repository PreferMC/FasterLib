package space.commandf1.FasterLib.run.gui;

import javax.swing.*;

public class Window extends JFrame {
    public Window(int x, int y) {
        this.setLocation(x, y);
        this.setTitle("哈哈");
        this.setSize(40, 40);
        this.setVisible(true);
    }
}
